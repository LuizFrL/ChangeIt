import { FilterByRegionPipe } from './filter-by-region.pipe';

describe('FilterByRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
